state_dir: /var/lib/vigil
socket_path: /run/vigil/vigil.sock
retention_days: 90
collectors_path: container/collectors.yaml
suppressions_path: container/suppressions.yaml
reporters_path: container/reporters.yaml
